# Commit note: updated image_features header

Short note: Small header change to `features/image_features.csv` to include `person_id` column.

Reason: clarifies which feature row belongs to which person.
