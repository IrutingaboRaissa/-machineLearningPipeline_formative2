# Commit note: added .gitignore entries

Short note: Update `.gitignore` to exclude large images, model checkpoints, and virtual env folders.

Reason: prevent accidental commits of large/generated files.
